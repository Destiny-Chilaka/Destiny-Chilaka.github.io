/** @type {import('tailwindcss').Config} */
export default {
    content: ['./index.html', './src/**/*.{js,jsx,ts,tsx}'], // Specify paths for Tailwind to scan
    darkMode: 'class', // Enable dark mode using a class
    theme: {
        extend: {},
    },
    plugins: [],
};